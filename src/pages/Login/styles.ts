import { Container } from 'react-bootstrap';
import styled from "styled-components";

export const LoginContainer = styled.div`
    height: 100%;
    width: 100%;
`

export const ContainerLoginForm = styled(Container)`
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`